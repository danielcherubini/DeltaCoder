# DeltaCoder v1.3 — Phase 1: Config Update + Re-Pretokenize

**Status:** ⏳ PENDING — waiting for v1.2 SFT training to complete

**Goal:** Update `pretokenize.py` and the Axolotl config to use `seq_len=32768`, then re-pretokenize the existing 230K dataset on the H200 instance.

**Why v1.3 exists:** v1.2 trained with `sequence_len: 8192`. The OCR dataset (nvidia/OpenCodeReasoning) averages 8,477 tokens per sequence with 50% of sequences exceeding 8,192 tokens. NVIDIA trained this dataset at 32K context. Training at 8192 truncated ~32,500 reasoning traces mid-thought — the model never saw the final answer for those examples. The H200 has 141GB VRAM but v1.2 only used 45GB. `seq_len: 32768` fits within VRAM, captures full OCR reasoning traces, and produces roughly 4x fewer but more token-dense steps — similar total training time, significantly better quality.

**Prerequisites:**
- v1.2 SFT training fully complete on H200
- v1.2 post-training steps done (merge, uploads, cache cleanup)
- H200 instance still alive at `root@213.5.130.43 -p 28988`
- `data/v1.2_sft_train.jsonl` already exists at `/workspace/DeltaCoder/data/v1.2_sft_train.jsonl` on the remote — **do not re-download or re-preprocess**
- `$HF_TOKEN` already set in `~/.bashrc` on remote (read/write token)

**The dataset does not change.** Same 230K rows, same 5 datasets as v1.2. Only the tokenization and training config change.

---

## Key Changes vs v1.2

| Parameter | v1.2 | v1.3 | Why |
|-----------|------|------|-----|
| `sequence_len` | 8192 | **32768** | Full OCR reasoning traces; H200 has headroom |
| `SEQUENCE_LEN` in pretokenize.py | 8192 | **32768** | Must match training config |
| Estimated steps | ~23,056 | **~5,800** | 4x larger windows = 4x fewer steps |
| Estimated step time (slow) | ~8.8s | **~25-30s** | Longer sequences, more compute per step |
| Estimated total time | ~46hrs | **~45-55hrs** | Fewer steps × longer time ≈ similar total |
| OCR truncation | 50% truncated | **0% truncated** | The whole point |

---

## Task 1: Update pretokenize.py for seq_len=32768

**Context:**
`scripts/pretokenize.py` has `SEQUENCE_LEN = 8192` hardcoded at line 20. This controls truncation during tokenization — sequences longer than SEQUENCE_LEN are truncated at line 131 (`input_ids = input_ids[:sequence_len]`). For v1.3 we must change this to 32768 so OCR reasoning traces are tokenized in full.

The script also uses multiprocessing by default (`min(mp.cpu_count(), 32)` workers). The Qwen3.5 tokenizer can be unstable with multiprocessing — pass `num_workers=1` explicitly when running.

**Files:**
- Modify: `scripts/pretokenize.py`

**What to implement:**

Change line 20 only:
```python
# OLD
SEQUENCE_LEN = 8192

# NEW
SEQUENCE_LEN = 32768
```

No other changes to the script.

**Steps:**
- [ ] Edit `scripts/pretokenize.py` line 20: change `SEQUENCE_LEN = 8192` to `SEQUENCE_LEN = 32768`
- [ ] Verify the change: `grep SEQUENCE_LEN scripts/pretokenize.py` should show `32768`
- [ ] Commit: `"update pretokenize.py: SEQUENCE_LEN 8192 → 32768 for v1.3"`

**Acceptance criteria:**
- [ ] `SEQUENCE_LEN = 32768` at line 20 of `scripts/pretokenize.py`
- [ ] No other lines changed

---

## Task 2: Create v1.3 Axolotl Training Config

**Context:**
Create `configs/deltacoder-9b-lora-v1.3.yaml` based on the v1.2 config (`configs/deltacoder-9b-lora-v1.2.yaml`) with `sequence_len` changed to `32768` and output paths updated to `v1.3`. All other parameters (LoRA rank, targets, learning rate, optimizer, attention, etc.) remain identical to v1.2 — they were validated and working.

**Files:**
- Create: `configs/deltacoder-9b-lora-v1.3.yaml`

**What to implement:**

```yaml
# Qwen3.5-DeltaCoder-9B v1.3 — LoRA SFT config for Axolotl
# Target: Vast.ai H200 141GB
#
# KEY CHANGE vs v1.2: sequence_len 8192 → 32768
# Rationale: OCR dataset (65K rows) averages 8,477 tokens, 50% exceed 8192.
# NVIDIA trained OpenCodeReasoning at 32K context. Training at 8192 truncated
# half the reasoning traces. H200 has 141GB VRAM (only 45GB used at 8192),
# so 32768 is comfortably within budget.
#
# Prerequisites on the training box:
#   (same as v1.2 — Axolotl template already has all deps)
#
# Pre-tokenize first (with SEQUENCE_LEN=32768 in pretokenize.py):
#   python scripts/pretokenize.py data/v1.2_sft_train.jsonl /dev/shm/train_tokenized_v1.3.jsonl 1
#
# Dry run FIRST to verify memory at 32768:
#   accelerate launch -m axolotl.cli.train configs/deltacoder-9b-lora-v1.3.yaml --max_steps=20
#
# Full run:
#   accelerate launch -m axolotl.cli.train configs/deltacoder-9b-lora-v1.3.yaml \
#     > logs/v1.3_axolotl_train.log 2>&1 &

base_model: Qwen/Qwen3.5-9B
trust_remote_code: true
strict: false

# ---------- Dataset ----------
datasets:
  - path: /dev/shm/train_tokenized_v1.3.jsonl
    ds_type: json
    type:

val_set_size: 0.02
dataset_prepared_path: last_run_prepared_v1.3
output_dir: ./outputs/deltacoder-9b-v1.3

# ---------- Sequence / Packing ----------
# 32768 captures full OCR reasoning traces (avg 8,477 tokens, max ~30K)
# NVIDIA trained OpenCodeReasoning at 32K — this matches their intent
sequence_len: 32768
sample_packing: true
pad_to_sequence_len: true

# ---------- LoRA ----------
adapter: lora
lora_r: 64
lora_alpha: 32
lora_dropout: 0.05
lora_target_modules:
  # Full Attention (8 of 32 layers)
  - q_proj
  - k_proj
  - v_proj
  - o_proj
  # GDN / Gated Delta Net (24 of 32 layers)
  - in_proj_qkv
  - in_proj_z
  - in_proj_b
  - in_proj_a
  - out_proj
  # MLP (all 32 layers)
  - gate_proj
  - up_proj
  - down_proj

# CRITICAL: disable LoRA kernel optimizations for Qwen3.5
lora_mlp_kernel: false
lora_qkv_kernel: false
lora_o_kernel: false

# ---------- Training ----------
num_epochs: 1
micro_batch_size: 1
gradient_accumulation_steps: 4
optimizer: adamw_torch
lr_scheduler: cosine
learning_rate: 1e-4
warmup_ratio: 0.05
weight_decay: 0.01

# ---------- Precision / Memory ----------
bf16: auto
tf32: true
gradient_checkpointing: true
gradient_checkpointing_kwargs:
  use_reentrant: false

# SDPA — flash_attention_2 causes CUDA errors with Qwen3.5 GDN layers
attn_implementation: sdpa

# ---------- Optimization ----------
# GDN doesn't support batch>1 with packing — group_by_length reduces padding waste
group_by_length: true

# ---------- Logging / Saving ----------
logging_steps: 10
evals_per_epoch: 4
saves_per_epoch: 2
save_total_limit: 3

# ---------- DO NOT USE ----------
# fix_untrained_tokens: true   # broken with Qwen3.5 (axolotl #2660)
# flash_attention: true        # use attn_implementation: sdpa instead
```

**Steps:**
- [ ] Create `configs/deltacoder-9b-lora-v1.3.yaml` with the content above
- [ ] Verify `sequence_len: 32768` is present
- [ ] Verify `output_dir: ./outputs/deltacoder-9b-v1.3` is set
- [ ] Verify `dataset_prepared_path: last_run_prepared_v1.3` (different from v1.2 to avoid cache collision)
- [ ] Commit: `"add deltacoder-9b-lora-v1.3.yaml: sequence_len 32768"`

**Acceptance criteria:**
- [ ] `configs/deltacoder-9b-lora-v1.3.yaml` exists
- [ ] `sequence_len: 32768`
- [ ] `output_dir: ./outputs/deltacoder-9b-v1.3`
- [ ] `dataset_prepared_path: last_run_prepared_v1.3`

---

## Task 3: Sync updated files to H200 and re-pretokenize

**Context:**
After committing locally, sync the updated `pretokenize.py` and new `v1.3` config to the H200 instance. Then re-run pretokenize with the new `SEQUENCE_LEN=32768`. The input dataset (`data/v1.2_sft_train.jsonl`) already exists on the remote — no need to re-upload the 3.8GB file.

The output goes to `/dev/shm/train_tokenized_v1.3.jsonl` (RAM disk). Estimated output size: ~25-35GB (sequences are longer now). Check `/dev/shm` has space first.

Note: use `num_workers=1` (pass as third argument) to avoid Qwen3.5 tokenizer multiprocessing issues.

**On H200 instance (connect with `ssh -T -o StrictHostKeyChecking=no -p 28988 root@213.5.130.43`):**

**Step 1: Sync files from local**
```bash
# From local machine — sync each file to its correct destination separately:
rsync -avP -e "ssh -T -o StrictHostKeyChecking=no -p 28988" \
    scripts/pretokenize.py \
    root@213.5.130.43:/workspace/DeltaCoder/scripts/

rsync -avP -e "ssh -T -o StrictHostKeyChecking=no -p 28988" \
    configs/deltacoder-9b-lora-v1.3.yaml \
    root@213.5.130.43:/workspace/DeltaCoder/configs/
```

**Step 2: Check /dev/shm total capacity and free space**
```bash
ssh -T -o StrictHostKeyChecking=no -p 28988 root@213.5.130.43 "df -h /dev/shm"
# Total capacity must be >= 35GB AND free space must be >= 35GB
# If total capacity < 35GB (e.g. only 32GB on low-RAM instances):
#   → Use /workspace instead: change output path to /workspace/DeltaCoder/data/train_tokenized_v1.3.jsonl
#   → Update dataset path in configs/deltacoder-9b-lora-v1.3.yaml to match
```

**Step 3: Run pretokenize (num_workers=1)**
```bash
# Default: output to /dev/shm (fast, requires 35GB capacity — verify in Step 2)
ssh -T -o StrictHostKeyChecking=no -p 28988 root@213.5.130.43 \
    "cd /workspace/DeltaCoder && nohup python scripts/pretokenize.py \
    data/v1.2_sft_train.jsonl \
    /dev/shm/train_tokenized_v1.3.jsonl \
    1 \
    > logs/pretokenize_v1.3.log 2>&1 & echo 'Started PID:' \$!"

# Monitor:
ssh -T -o StrictHostKeyChecking=no -p 28988 root@213.5.130.43 \
    "tail -f /workspace/DeltaCoder/logs/pretokenize_v1.3.log"
```

Expected: ~20-30 minutes. Output size ~25-35GB.

**Fallback if /dev/shm is too small:** Change output path to disk:
```bash
ssh -T -o StrictHostKeyChecking=no -p 28988 root@213.5.130.43 \
    "cd /workspace/DeltaCoder && nohup python scripts/pretokenize.py \
    data/v1.2_sft_train.jsonl \
    data/train_tokenized_v1.3.jsonl \
    1 \
    > logs/pretokenize_v1.3.log 2>&1 & echo 'Started PID:' \$!"
# Then update dataset path in configs/deltacoder-9b-lora-v1.3.yaml:
# path: /workspace/DeltaCoder/data/train_tokenized_v1.3.jsonl
```

**Step 4: Verify**
```bash
wc -l /dev/shm/train_tokenized_v1.3.jsonl
# Should be ~229,959 rows (same as v1.2)

head -1 /dev/shm/train_tokenized_v1.3.jsonl | python3 -c "
import json, sys
row = json.load(sys.stdin)
print('input_ids len:', len(row['input_ids']))
print('labels len:', len(row['labels']))
print('max possible:', 32768)
"
# input_ids len should be <= 32768
```

**Acceptance criteria:**
- [ ] `/dev/shm/train_tokenized_v1.3.jsonl` exists on H200
- [ ] Row count ~229,959
- [ ] No sequence exceeds 32768 tokens
- [ ] Verify a known-long OCR sequence is longer than 8192 tokens now (previously it would have been exactly 8192)

---

## Estimated Timeline

| Step | Time |
|------|------|
| Code changes + commit (local) | ~5 min |
| Sync to H200 | ~2 min |
| Re-pretokenize (num_workers=1) | ~20-30 min |
| **Total Phase 1** | **~35 min** |
