# DeltaCoder v1.2 — Redo Plan (REVISED)

**Goal:** Redo v1.2 SFT + DPO at **32K context** with proper tool-calling data, targeting the BEST 9B for (1) Coding, (2) Tool Calling, (3) Agentic Workflows. Must preserve vision capabilities.

**Architecture:** Unsloth FastVisionModel (text-only LoRA with packing) → SFT at 32K on H100 → validate → DPO → merge → GGUF → test on Kronk → upload to HuggingFace.

**Tech Stack:** Unsloth (SFT), vLLM nightly (DPO pair gen), TRL (DPO training), ik_llama.cpp (GGUF export)

---

## What went wrong in the first attempt

1. `normalize_messages()` in `pretokenize.py` corrupted ~65K+ tool-calling examples (converted structured `tool_calls` into raw text)
2. Trained at 8K context (truncated reasoning traces)
3. No validation gate before DPO — wasted money on degraded DPO pairs

## What changed in the redo

1. **32K context** instead of 8K — full reasoning traces preserved
2. **262K training rows** (rebuilt mix: 30.5% nemotron_tool_calling, 19.1% opencoder_reasoning, 15.3% code_feedback, 11.5% swesmith, 11.5% magicoder, 6.5% nemotron_agentic, 5.7% xlam)
3. **Unsloth instead of Axolotl** — Axolotl OOMs at 32K (Liger fused CE doesn't patch VL model), Unsloth handles it internally
4. **FastVisionModel + finetune_vision_layers=False** — preserves vision weights while only training language layers
5. **VLM packing unblock** — Unsloth blocks packing for VLMs; we patch trainer.py to remove the `is_vlm` check (validated in unslothai/unsloth#4160)
6. **Persistent volume** — 300GB Vast.ai volume at `/workspace/` survives instance destruction
7. **Validation gates** — test SFT model before DPO, test on Kronk before uploading

---

## Infrastructure

### Instance: H100 SXM 80GB + 300GB Volume
- **Image**: `nvcr.io/nvidia/pytorch:26.01-py3` (PyTorch NGC)
- **Volume**: 300GB at `/workspace/` (persists across instance swaps)
- **Venv**: `/workspace/venv/` (on volume, persists)
- **Cost**: ~$1.67/hr GPU + ~$2/day storage

### Why Unsloth (not Axolotl)
| Aspect | Axolotl | Unsloth |
|--------|---------|---------|
| 32K OOM | YES — Liger fused CE only patches ForCausalLM, not ForConditionalGeneration | NO — handles fused CE internally |
| VLM support | Loads as ForConditionalGeneration, but loss function OOMs | FastVisionModel preserves vision + fused CE works |
| Packing | Works out of the box | Blocked for VLMs — needs 2-line patch |
| Multi-GPU | FSDP2 works (minus OOM) | Single GPU only |

### Key Files on Volume
```
/workspace/
├── venv/                    # Python venv
├── v1.2_sft_train.jsonl     # 262K rows, 5.1GB
├── train_unsloth.py         # SFT script (FastVisionModel + packing)
├── patch_vlm_packing.py     # VLM packing unblock
├── outputs/                 # LoRA adapters + checkpoints
├── merged_v1.2/             # Merged SFT model (~18GB)
└── logs/
```

---

## Tasks

### Task 1: Setup Instance ✅
- [x] Create H100 SXM instance with 300GB volume
- [x] Upload training data (262K rows, 5.1GB) to `/workspace/`
- [x] Upload scripts to `/workspace/`

### Task 2: Install Dependencies & Patch
- [ ] Create venv on volume: `uv venv /workspace/venv`
- [ ] Install unsloth + deps: `uv pip install unsloth causal-conv1d flash-linear-attention`
- [ ] Run `python /workspace/patch_vlm_packing.py` to unblock VLM packing

### Task 3: Dry Run (20 steps)
- [ ] `python /workspace/train_unsloth.py --data /workspace/v1.2_sft_train.jsonl --max-steps 20`
- [ ] Verify: packing enabled, loss decreases, no NaN grads, VRAM < 80GB
- [ ] If NaN at batch_size=1: try gradient clipping, lower lr, or fall back to no-packing

### Task 4: Full SFT Training
- [ ] `nohup python /workspace/train_unsloth.py --data /workspace/v1.2_sft_train.jsonl --output-dir /workspace/outputs/deltacoder-9b-v1.2 > /workspace/logs/sft_train.log 2>&1 &`
- [ ] Monitor: `tail -f /workspace/logs/sft_train.log`
- [ ] Expected: ~262K rows / ~38 hours with packing
- [ ] Merge LoRA into base model → `/workspace/merged_v1.2/`

### Task 5: Validate SFT (STOP/GO gate)
- [ ] Serve merged model via vLLM nightly
- [ ] Test 5+ prompts: coding, tool calling, multi-turn, reasoning
- [ ] STOP if: degenerate loops, bad tool calling, no EOS

### Task 6: DPO
- [ ] Generate on-policy DPO pairs from AceCode-V2-122K
- [ ] Train DPO with TRL
- [ ] Merge DPO LoRA

### Task 7: GGUF + Kronk Test
- [ ] Merge + export GGUF on Romulus
- [ ] Test Q4_K_M on Kronk
- [ ] User approves → full quantization + HF upload

---

## Estimated Cost

| Step | Time | Cost |
|------|------|------|
| SFT training (with packing) | ~38hrs | ~$63 |
| DPO pair gen | ~4hrs | ~$7 |
| DPO training | ~2-4hrs | ~$5 |
| Volume storage (~5 days) | — | ~$10 |
| GGUF + Kronk | — | $0 (Romulus) |
| **Total** | **~48hrs** | **~$85** |
